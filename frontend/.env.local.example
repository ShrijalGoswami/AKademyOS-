# AKademyOS frontend — committed template. Copy to .env.local and fill in real values.
# Keep the SAME keys here as in .env.local (values stay empty in this template).
# Only NEXT_PUBLIC_* vars are exposed to the browser — never put secrets here.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
