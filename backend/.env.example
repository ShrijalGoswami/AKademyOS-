# AKademyOS backend — committed template. Copy to .env and fill in real values.
# Keep the SAME keys here as in .env (values stay empty in this template).

# --- Supabase (required) ---
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_JWT_SECRET=

# # --- Tutor microservice (optional until wired up) ---
# TUTOR_URL=
# TUTOR_API_KEY=
